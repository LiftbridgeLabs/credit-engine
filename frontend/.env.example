# Only set this if the frontend and backend are on different hosts — otherwise leave it unset
# and the app will auto-detect the backend at the same host it's being viewed from, port 8000.
# VITE_API_BASE_URL=http://192.168.1.x:8000
